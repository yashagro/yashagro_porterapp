import 'package:flutter/material.dart';

class DealersHomeScreen extends StatelessWidget {
  const DealersHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Dealers Home page")));
  }
}